## What this changes

<!-- One or two sentences. Link the issue if there is one. -->

## How it was verified

- [ ] `npm run build`
- [ ] `cargo test --manifest-path src-tauri/Cargo.toml`
- [ ] Ran the app and looked at the change

<!-- UI changes: WebKit renders composited layers differently inside a
     transparent window, so a browser screenshot is not proof. Please check
     `npm run tauri dev` and say so here. -->

## Notes for the reviewer
